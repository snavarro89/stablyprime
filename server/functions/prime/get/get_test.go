package prime

import "testing"

func TestZero(t *testing.T) {
	/*prime, err := handler()
	expect := 0
	if err != nil || prime != 0 {
		t.Fatalf(`GetPrime(0) = %b, %v, want match for %b, nil`, prime, err, expect)
	}*/
}
